# Moved: Deployment QA

Canonical deployment, validation, live verification, and closeout rules now live in `agent-docs/DEPLOYMENT-QA.md`.

Agents must read `AGENTS.md` first, then follow the routing table to the relevant `agent-docs/` files.
