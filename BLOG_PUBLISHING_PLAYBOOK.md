# Moved: Blog Publishing Playbook

Canonical blog publishing rules now live in `agent-docs/BLOG-PUBLISHING-PLAYBOOK.md`.

Agents must read `AGENTS.md` first, then follow the routing table to the relevant `agent-docs/` files.
