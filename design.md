# Moved: Design System

Canonical visual system, accessibility, image, and UI rules now live in `agent-docs/DESIGN.md`.

Agents must read `AGENTS.md` first, then follow the routing table to the relevant `agent-docs/` files.
